services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: aitrainers
      POSTGRES_PASSWORD: aitrainers
      POSTGRES_DB: aitrainers
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aitrainers -d aitrainers"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
